# Data pipeline dependencies (OCR + image processing)
# Install with: pip install -r requirements-data.txt
# Note: paddleocr has heavy dependencies (~2GB). Install separately if needed.
paddleocr
opencv-python==4.12.0.88
numpy==2.2.6
scikit-learn
pandas
